import { EventTypeDirective } from './event-type.directive';

describe('EventTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new EventTypeDirective();
    expect(directive).toBeTruthy();
  });
});
